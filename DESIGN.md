# Design

Read this before designing, specifying, or implementing a user-facing surface. PRDs cite this file's names and states; a PRD that defines a visual is exposing a missing entry here.

The delta over Goose Desktop's design system. Under a section upstream already settles the line is "upstream's, unchanged" and only what the fork adds follows. Headings tagged `[contract]` are matched by code and carry a `check:` line; a contract without one is listed under Open decisions. Everything else is direction. Template: `skills/rpi/templates/design.md`.

## Principles

- **The Upstream Rule** — chat, tool rows, settings and the session list are Goose's components, composed not restyled (`ARCHITECTURE.md:86`, `:98`); a fork-side copy of an upstream component is a bug.
- **Starts as a chat** (2026-09-16, task 58; user: "have an easy mode and advanced mode") — Easy is the default; every control beyond the lever and the three pane icons waits behind Advanced (the ⋯ menu's "Advanced controls", or Settings › App). Easy → Advanced keeps the session; Advanced → Easy puts the lever on the stop whose triple the session matches, else Custom.
- **The One Dock Rule** (2026-09-15, supersedes the One Pane Rule; tasks 41–42) — the centre is the chat and nothing else; every pane lives in the Work column, and at most two show at once (2026-09-16, task 71: one full, or the top and bottom halves — `pane-store.ts` `Slots`, `WorkColumn.tsx`; until then a stack of panels, each a tab strip over one visible pane); a pane beside the chat outside the column, a floating window, or a modal pane is forbidden.
- **The Nothing Lost Rule** — a pane keeps its state while it is anywhere in the Work column: a tab docked to another position, parked out of its slot, or resized is the same mounted pane (`pane-store.ts` `tabs`, `WorkColumn.tsx` renders every open pane once); a pane that remounts empty after a move is a bug. A closed pane leaves the column, and reopening it opens it fresh — the position is not kept; the tab bar is the record of what is open, not of what was (2026-09-16, task 71: an open tab out of its slot keeps its position and one click brings it back there).
- **The Named Runtime Rule** — every step on screen says which runtime did it, in the runtime's own name: the Runtime chip (the header until task 60), the "→ Codex from here" divider, the worker row (PRD `:21-24`, `:96-97`; PRODUCT.md §11); colour or an icon alone never carries it.
- **The Floating Button Rule** `[direction]` (user, 2026-09-15) — buttons carry a soft shadow beneath them so they read as floating; a flat control the user is meant to press is a bug.
- **The Into Rule** `[direction]` (user, 2026-09-15) — transitions have things disappear *into* things: a closed pane shrinks into its strip, an opened pane grows out of it, a torn-off tab lands where it was dropped; a cut, a fade to nothing, or an element that appears from nowhere is forbidden where a source or destination exists. 2026-09-16 (task 56): the Browser's suggestion list grows out of the address bar and closes back into it. 2026-09-16 (task 71): a closed tab disappears into its launcher — the same tab, unpressed, on the bar — and a pane leaving a slot disappears into its tab; a dragged tab lands in the half it was dropped on.
- **Surfaces float** `[direction]` (user, 2026-09-16) — the Floating Button Rule extended to surfaces: elevation separates, outlines don't. In dark, every card, panel and popover carries `--shadow-sm` at rest and `--shadow-md` lifted, and the `border-*` tokens are near-invisible hairlines; a surface that separates itself with a line is a bug.
- **The Glass Rule** `[direction]` (user, 2026-09-16) — a surface is glass only when something scrolls or shows behind it: a column (2026-09-16, task 60: the header and the dock panel until then; the rail's buttons until task 71 — the tab bar has nothing behind it), the chat-input card, a menu over the desk or the transcript; a glass surface with nothing behind it is a bug. Text never sits on glass without the surface's own background at ≥ .72 alpha — the blur is under the surface, never under the letters (`main.css` "Charcoal Monokai", `theme-tokens.test.ts`).
- **The One Word Rule** — one word per concept per tier (§Vocabulary); the fork's "Mode" never means Goose's permission gate, and a provider id never reaches the default surface.

## Frame

Region names are the `ARCHITECTURE.md` §Modules they render into (`workspace`, `components`); a region without a module, or a module drawing outside its region, is drift. `main`, `preload`, `acp`, `native` and the sidecar render nothing and have no region (`ARCHITECTURE.md:80-88`).

```text
Application window — three columns, no top bar (2026-09-16, task 60; user: "please remove the top navbar", "3 vertical panels")
├── titlebar — the 32 px drag strip: traffic lights and upstream's sidebar toggle only; no background, no controls (`main.css` .titlebar-drag-region)
├── Sessions — `components` wrapped by `workspace`: upstream's sidebar (`NavigationPanel.tsx` Navigation), open by default, sessions grouped by project;
│              collapses into the titlebar toggle (upstream's `NavigationContext`, also under 700 px); width 280 px by default (`pane-store.ts` DEFAULT_COLUMNS)
├── seam — drag or arrow to resize the column beside it (`WorkspaceShell.tsx` Seam); widths are remembered per project (`goose.workspace.columns`)
├── Chat — `components`: the chat (transcript, tool rows, input with ⌘Enter), the Hub, or any other page; never a pane; takes what the others leave, never under 240 px
│   ├── RPI strip — `workspace`, above the chat (task 29, landed 2026-09-16; PRD :127-134)
│   └── chips — `workspace` in the chat card's bottom row, left of the model and directory chips (`ChatInput.tsx` SessionChipsSlot):
│                Easy (default, task 58): the lever alone — Easy · Medium · Hard, the knob's label under it (`Lever.tsx`)
│                Advanced: Runtime ▾ · Mode ▾, each a popover with Install / "no orchestrator role" kept (`SessionChips.tsx`), then Session controls ▾ —
│                a popover listing every ACP config option the server publishes, the working directory (Open in Files), the role, the enabled
│                extensions (→ Extensions) and Save as routine… (`SessionControls.tsx`); the chips row is one flex line, so the popover is the strip's only form
├── seam
└── Work — `workspace`: the tab bar over the slots (`WorkColumn.tsx`), 480 px by default; always there, in Easy and Advanced alike — the one place
    │        pane controls live (rewritten 2026-09-16, task 71; user: "have a permanent navbar at the top that creates the tabs … you can dock the
    │        entire right panel, half the top or half the bottom"; until then task 42's stack of panels and task 60's floating rail)
    ├── tab bar — pinned to the top of the column, every pane launcher as a tab (icon + label): Terminal · Changes · Browser first, then Files ·
    │             Editor · Git · Markdown · Artifact · Agents as they exist (`WorkspaceShell.tsx` PRIMARY_PANES, then `PANE_IDS`); under ~520 px the
    │             tabs that are neither first nor open fold under a "more" chevron; the session menu's ⋯ (task 69) at the right end
    │             a click opens the pane as an open tab (pressed while it shows, × on hover; a dot for what arrived while it was hidden, task 69);
    │             an open tab out of a slot stays open, its state kept, and one click brings it back into the position it last showed in
    │             right-click (long-press) an open tab → Full · Top half · Bottom half · Close; drag a tab → a ghost, and the column's top or
    │             bottom half lights as the drop zone (`WorkColumn.tsx` drag)
    └── slots — at most two panes show at once: one pane **full** (the whole column), or a **top half** over a **bottom half** with the seam between
                (`pane-store.ts` `Slots`, `size`); a half whose partner empties takes the column
                the pane header: icon · name · the three-icon dock-position control (§Iconography) · ×
                open a second pane while one is full → the new one takes the bottom half, the full one moves up (`pane-store.ts` openPane)
                drag or arrow the seam → resize the halves against each other (`resize`)
                the column is remembered per project, keyed by the window's working dir (`WorkColumn.tsx` loadDock/saveDock, `pane-store.ts` restoreDock)
                later: Agents (PRD :118-140; PRODUCT.md §11)

Keys: ⌘1 · ⌘2 · ⌘3 focus Sessions · Chat · Work (Work with nothing open is the bar's first tab).

Phone width (≤ PHONE_MAX_WIDTH_PX, `pane-store.ts:18`, `:43-45`)
└── tab rail — chat first, then the same tabs; one thing visible, no split (`pane-store.ts:35`; PRD :142-152)
```

- The chat keeps its location and meaning at every width; every control lives on the thing it controls (the Into Rule) — nothing sits between the user and the conversation; the Hub and the spotlight launcher are upstream's, unchanged (PRD `:28-30`).
- As space contracts: Sessions and Work give way first (they shrink; upstream folds the sidebar under 700 px), the frame clamps to the viewport (`overflow-x: hidden` on the shell, `min-w-0` on every column) and the chat never shrinks below one readable line and its input (240 px); at phone width the Work column folds away whole behind the chat, its open tabs kept (`pane-store.ts` setMode). Growing back shows the column as it was, plus the pane the phone had on screen (`pane-store.test.ts` "folds the column away").
- Dense surfaces: pane contents (tree, diff, terminal, git lists) at upstream's compact sizes; the chips, the tab bar, tabs and chat are never dense.
- Overlays: upstream's stack, unchanged — dialogs (`ui/desktop/src/components/ui/dialog.tsx`) above toasts (`react-toastify`, `ui/desktop/package.json:96`); the fork adds no overlay. A pane is never an overlay. Focus returns to the control that opened the dialog; Esc dismisses a dialog, and only a dialog (see §Accessibility).

## Vocabulary & copy [contract]

| Concept | Default surface | Diagnostics | Internal |
|---|---|---|---|
| who the session talks to | **Runtime** — Claude · Codex · Cursor · agy · More… (PRD `:37-39`; agy from PRODUCT.md §5) | provider | the session's `provider` config option (`ARCHITECTURE.md:103`); ids `claude-acp`, `claude-code`, `codex-acp`, `cursor-acp`, `agy` (PRODUCT.md §5) |
| whether the runtime orchestrates | **Mode** — Direct · Orchestrate (PRD `:38-39`) | mode | whether the first turn loads `.agents/agents/orchestrator.md` (PRD `:51-52`) |
| Goose's permission gate | **Autonomous · Manual …**, in Settings only (`components/settings/mode/ModeSelectionItem.tsx:10`, `:18`) | goose mode | `GOOSE_MODE` (`components/settings/mode/ModeSection.tsx:13`) |
| talk to one runtime, no role | **Direct** | direct | no role loaded (PRD `:52`) |
| Claude owns the request, delegates | **Orchestrate** | orchestrate | orchestrator role loaded (PRD `:51`; PRODUCT.md §4) |
| a tool surface | **pane** — Files · Editor · Changes · Terminal · Git · Browser · Markdown · Agents (task 28) · Artifact (task 30) · Review (task 70) · Telemetry (task 128) | pane | `PaneId` (`pane-store.ts:4`); the Changes pane's id stays `diff` |
| the Browser's controls | **Back · Forward · Refresh · Share** (2026-09-16, task 56); Refresh reads **Stop** while the page loads; Share is **Share with agents**, a labelled button (2026-09-20, user). Amended 2026-09-19 (task 87): Share is a menu — **Page · Screenshot · Console** — each landing in the chat input through the one door (Add to chat); **Set as home** on the address row keeps the page the pane opens on, per project | browser toolbar | `browserPane.*` message ids; `data-testid="browser-back"` … `browser-share` (`panes/browser/BrowserPane.tsx`) |
| the window's three parts | **Sessions · Chat · Work** — the columns (task 60) | column | `workspace-column-sessions` / `-chat` / `-work` test ids; `Columns` (`pane-store.ts`) |
| the Work column's launchers (2026-09-16, task 71) | **tab bar** — Amended 2026-09-20 (tasks 117–120, user: "1 or 2 tab bar menu … add a tab with the +"): **one bar per panel**, holding only that panel's open panes as attached tabs, each with × and a right-click menu; **+** at the bar's end lists the panes not open (icon, name, the unseen dot); the ⋯ session menu on the first bar; an empty column shows one bar with + and ⋯ over a hint; a drag onto a half makes the second panel with an identical bar; a panel whose last tab closes goes. Retired: **launcher** (a pane's permanent spot on the bar), the fold chevron and `primary` — a tab is what is open. Before the amendment: pinned to the top of the Work column, every pane a **tab**: Terminal · Changes · Browser first, then the rest; an open tab is pressed while its pane shows, carries × on hover, and stays on the bar (open, its state kept) when its pane leaves a slot; the ⋯ session menu at the right end; under ~520 px the tabs that are neither first nor open fold under a "more" chevron | bar | `WorkColumn.tsx`; `tabs` (`pane-store.ts`); `workspace-pane-menu`, `workspace-side-tab-*`, `workspace-pane-button-*` (`[data-open]`, `[aria-pressed]`), `workspace-pane-close-*`, `workspace-pane-overflow`, `workspace-pane-overflow-*`, `workspace-tab-menu`, `workspace-tab-close` test ids |
| where an open pane shows (2026-09-16, task 71) | **dock position** — **Full** · **Top half** · **Bottom half**: the tab's right-click menu, or a drag onto the column's top or bottom half; at most two panes show at once, a Full pane takes the whole column, a half whose partner empties takes the column. Amended 2026-09-20 (tasks 113–116, user: "only one tabbed panel … split by drag"): **one pane at a time by default** — opening a pane takes the column and parks what showed; a split happens only by a drag (or the menu), never by a second open; a parked half returns beside its partner only while that partner still shows; closing the pane showing hands the column to the tab before it. The header's three position icons are retired (the menu keeps the keyboard path). **Tabs are attached** (task 114): the pressed tab shares the panel's ground and covers the strip's hairline, 13 px label with icon; the strip sits on the secondary ground; tabs are not Floating Buttons | position | `DockPosition`, `Slots`, `dock` (`pane-store.ts`); `workspace-side-panel`, `workspace-pane-*[data-position]`, `workspace-dock-positions`, `workspace-dock-position-full` / `-top` / `-bottom`, `workspace-dock-drop-top` / `-bottom`, `workspace-dock-ghost` test ids |
| the line between the two halves | **seam** | seam | `resize`, `size` (`pane-store.ts`); `workspace-dock-seam` test id |
| the session's actions (2026-09-16, task 69) | **session menu** — the bar's ⋯ (the rail's until task 71), laid out as Claude Code desktop's: the panes (Files ⇧⌘F · Editor · Git · Markdown) · **Background tasks** (the Runs inbox until the Board lands) · **Open in ▸** (Finder · Terminal · Code editor) · **Rename** R · **Fork** F · **Transcript view ▸** (Full · Compact, then View session JSON · View recent model interactions) · **Output style ▸** (the Response styles, app-wide, and says so) · **Keep computer awake** "Only for this session" · Advanced controls · Open on phone… · Save as routine… · **Archive** A · **Delete** D (`danger`, behind a confirm); the letters fire while the menu is open; the web build shows it minus Open in ▸ and Keep computer awake; upstream's header ⋯ is hidden on the workspace routes | session actions | `RailMenu.tsx`, `hooks/useSessionActions.ts`, `SessionActionDialogs` (`SessionActionsHeader.tsx`); `keep-awake` and `open-in-editor` IPCs; `workspace-pane-more-menu`, `workspace-background-tasks`, `workspace-open-in`, `workspace-open-in-finder` / `-terminal` / `-editor`, `workspace-rename`, `workspace-fork`, `workspace-transcript-view`, `workspace-transcript-full` / `-compact`, `workspace-view-json`, `workspace-view-model-interactions`, `workspace-output-style`, `workspace-output-style-<key>`, `workspace-response-styles`, `workspace-keep-awake`, `workspace-archive`, `workspace-delete`, `session-rename-dialog` / `-input` / `-save` test ids; `localStorage['goose.transcriptView:<id>']`, `workspace-column-chat[data-transcript-view]` |
| what arrived while a pane was hidden (2026-09-16, task 69) | **dot** — `info`, top-right of the tab (the rail icon until task 71), its tooltip reading "<pane> — new since you looked": Changes when the working tree changed since the pane last showed it (a `/git/status` poll every 30 s while hidden), Browser when a page finished loading, Terminal when the shell wrote; opening the pane clears it | unseen | `unseen` on `PaneLayout`, `markUnseen` (`pane-store.ts`), `markUnseen` on `PaneContext`; `workspace-pane-button-*[data-unseen]`, `workspace-pane-dot-*` test ids |
| a session control in the chat card's bottom row | **chip** — Runtime · Mode beside upstream's model and directory chips (task 60). Amended 2026-09-20 (tasks 121–124, user: "there is a lot of noise"; option A of the composer mockups): on the workspace route the row is quiet — **Easy** shows the lever, the folder chip, attach and send, nothing else; **Advanced** adds the **model chip** (the cube glyph and the model id in mono) and the Runtime · Mode chips; cost, the token count, the extensions count and diagnostics live in Session controls (Cost and Diagnostics rows); the lever's stop word is screen-reader-only, its tooltip the visible name. Amended 2026-09-21 (tasks 140–141, user: "the ux is rough" → option A, "Easy is quiet"): the two rows as written — **Easy**: lever · folder · attach · send (the mic only while it records; the Worktree and Routine chips are Advanced's); **Advanced**: seat · mode · worktree · folder · branch · model, then Session controls · attach · mic · send — Session controls sits with the icon-only controls, not between chips. **One glyph, one meaning** (ux_tests `DESIGN_LANGUAGE.md` §3.1: "One icon per control"; "an icon is a mark beside a word"): the cube is the model's, the seat chip draws the command-line glyph, the mode chip the four squares. One face on the row: every chip label in the row's sans; the model id alone is mono (it is the machine's word); `current` — an ACP seat's name for its own default — is never shown, the chip reads the resolved model or hides | chip | `SessionChips.tsx`, `WorkspaceComposerSlot` (`ChatInput.tsx`); `workspace-runtime`, `workspace-mode`, `model-chip`, `workspace-config-cost`, `workspace-config-diagnostics` test ids |
| how much of a limit is spent (2026-09-20, task 122) | **usage ring** — a ring on the send disc's edge that fills to the **most spent limit**: the context window, and a seat's plan windows once the seat reports them; `background-inverse` while filling, `warning` from 80 %, `danger` at 100 %; a hairline alone while no limit is known; the exact numbers are its tooltip | meter | `UsageRing.tsx`, `usage-ring.ts` (`mostSpent`, `ringState`, `RING_STATES`); `usage-ring[data-state][data-warm]`, `role=meter` `aria-valuenow` |
| the ring, opened (2026-09-20, task 122) | **usage breakdown** — hover or focus the disc: **Context window** `used / limit · pct%` with a bar, "Compacts automatically at 97%" · **Compact session**; **Plan usage** with one row per limit a seat reports (bar, reset, percent) or "Plan limits: not reported by <seat>"; **See detailed breakdown** → Session controls; Esc closes; the disc still sends | breakdown | `usage-breakdown`, `usage-limit-<id>`, `usage-compact`, `usage-not-reported`, `usage-see-breakdown` test ids; walk `usage ring` |
| the workspace's two faces | **Easy** · **Advanced** — "Advanced controls" is the toggle's label (task 58) | ui | `workspace.ui` setting (`utils/settings.ts` `WorkspaceUi`); `workspace-shell[data-ui]`, `workspace-advanced-controls`, `settings-advanced-controls` test ids |
| the phone's door | **Phone** card in Settings › App — the tailnet address as text with **Copy** and as a QR code, and the **Port** (task 62); the ⋯ menu's **Open on phone…** lands on it | phone URL | `sidecar.port` setting (`utils/settings.ts`, default 7788); `get-phone-url` IPC; `settings-phone`, `settings-phone-url`, `settings-phone-copy`, `settings-phone-qr`, `settings-phone-port`, `workspace-open-phone` test ids |
| the Sessions column's rail (2026-09-22, tasks 148–151, user: "organize this left side … reduce the amount of needed groups. add search and sorting" → option C → mockup C3) | **tab bar** — four tabs at the rail's foot, icon over a 10 px label: **Chats · Library · Automate · Settings**; the tab is remembered; Settings is a route. Chats: **search** first with **New chat** (`+`, ⌘N) beside it; a row of **repo chips** — All · one per repository · **Elsewhere** (scratch and temp dirs) — each with its count; the list flat, grouped by day (Today · Yesterday · weekday · date) with the **sort** control (Recent · Name · Project) on the first heading; a row is its title, time and unread dot, then its repo and `wt/<slug>` (mono) when it is a worktree's; **Show all** → Session History. Library: Recipes · Skills · Apps · Extensions. Automate: Board · Scheduler. Never "Session History" as a row, never a heading per checkout | tab bar, repo chip, Elsewhere | `NavigationPanel.tsx`, `sidebar-sessions.ts`; `sidebar-tab-*`, `sidebar-search`, `sidebar-new-chat`, `sidebar-chip-*`, `sidebar-sort`, `sidebar-day-*`, `sidebar-session-*`, `sidebar-show-all` |
| Easy's one control | **lever** — never "difficulty", "power" or "slider" in copy | lever | `Lever.tsx`; `workspace-lever`, `workspace-lever-stop-*`, `workspace-lever-knob`, `workspace-lever-label` test ids |
| a position on the lever | **stop** — **Easy** · **Medium** · **Hard**, and **Custom** (read-only, when the session matches none) | stop | `Stop`, `LEVER`, `stopOfSession` (`session-controls.ts`); the (provider, model, mode) triple |
| Advanced's list of everything the session has | **Session controls** — Provider · Mode · Model · Thinking effort in the server's own names, then Working directory · Role · extensions · Save as routine… | config options | `SessionControls.tsx`; `workspace-session-controls`, `workspace-config-<id>`, `workspace-config-<id>-<value>` test ids; `src/acp/sessionConfig.ts` |
| the phone's one-at-a-time strip | **tab rail** | rail | `visible` (`pane-store.ts:35`) |
| delegated work | **worker** — runtime · role · task · status (PRD `:103-104`); amended 2026-09-16 (task 28): the **Agents** pane's **worker row** — title, the status word beside its dot (**Waiting · Running · Done · Failed**; **No status recorded** on a row seeded from the session record), the role file's name (**Ad hoc** when none), then the child record's provider id · model in mono as machine text (the Worktree chip's `wt/<slug>` rule) until task 5's payload carries the picked runtime's written name; a click opens the worker's transcript in place, **Read-only**, with **Back**; a running row reads **Opens once the worker is done** in place | subagent | SubAgent session; `src/acp/delegations.ts` rows keyed by `subagentSessionId` (task 65); `agents-pane[data-state]`, `agents-root`, `agents-tree`, `agents-row[data-status|data-provider|data-model]`, `agents-row-open`, `agents-row-role`, `agents-row-runtime`, `agents-row-error`, `agents-empty`, `agents-back`, `agents-transcript` test ids (`panes/agents/AgentsPane.tsx`, `panes/agents/AgentRow.tsx`) |
| the job a worker does | **role** — Orchestrator, Researcher, Planner, Implementer, Reviewer, Advisor (and the Advisor's four specialists) | role | the ten files in `.agents/agents/` (`ARCHITECTURE.md:94`) |
| what a worker hands back | **artifact** — Brief · Plan · Result · Review (PRD `:113-115`) | artifact | the markdown the worker returned |
| a runtime switch mid-session | **"→ <Runtime> from here"** divider (PRD `:96-97`) | handoff | compacted handoff memo (PRODUCT.md §4) |
| a chat's own checkout (2026-09-16, task 49) | **worktree** — the Worktree chip, off by default; in a session the chip and the Changes pane's row read the branch `wt/<slug>`, mono (machine text) | worktree | `<toplevel>/.worktrees/<slug>` on `wt/<slug>` (`ui/sidecar/src/git.ts`); `workspace-worktree`, `diff-worktree`, `diff-branch`, `diff-merge`, `diff-remove-worktree` test ids |
| the checkout a worktree merges into (2026-09-16, task 49) | **main checkout** — the Changes pane's **Merge into <branch>** names its branch; **Remove worktree** hands the chat back to it | main checkout | the first entry of `/git/worktree/list`; `session/update_working_dir` on Remove |
| the branch, shipped for review (2026-09-16, task 66) | **PR** — the Git pane's section under the commit box: **Push and open PR…** until the branch has one (the sheet's fields are **Title · Body · Draft**, its action **Create**), then `#<number>` (mono, opens on GitHub) · **Open · Draft · Merged · Closed** · **Checks** with one row per check reading **passed · failed · pending · skipped**; the Changes pane's worktree row offers the same as **Open PR…** beside Merge once the branch has an upstream; a missing or logged-out `gh` reads **Install** / **Sign in** (§States, Unavailable) | pull request, gh | `POST /git/pr/create`, `/git/pr/status`, `/git/push`, `/git/log` (`ui/sidecar/src/git.ts`, `gh` via `execFile`); `git-pr`, `git-pr-open`, `git-pr-blocker`, `git-pr-link`, `git-pr-state`, `git-pr-checks`, `git-pr-check[data-check-state]`, `git-pr-error`, `git-pr-install`, `git-pr-sign-in`, `pr-sheet`, `pr-title`, `pr-body`, `pr-draft`, `pr-create`, `diff-open-pr` test ids (`panes/git/GitPane.tsx`, `panes/git/PrSheet.tsx`) |
| a second model's judgment of a branch (2026-09-16, task 70) | **review** — **Review branch…** in the Changes pane's worktree row (beside Merge) and in the Git pane's PR section starts a **review session**: a normal session on the Reviewer role's rolled seat, titled **Review: <branch> vs <base>** (the tag; the ACP client writes no `extension_data`), whose first prompt asks for the role's Return shape; the **Review** pane shows the reply — header `<branch> · <base> · <Runtime> · <model>`, **Re-review** (the same prompt to the same session), **Open transcript**; sections **Requirements coverage · Plan adherence · Correctness issues · Architecture concerns · Test gaps · Recommended fixes**, each collapsible, every `file:line` a link that opens the Editor at that line; several reviews of the cwd list above the newest; no Apply — the Reviewer never fixes (its rule), so the pane offers nothing that would | review session, reviewer | `panes/review/` (`ReviewPane.tsx`, `review-parse.ts`, `review-state.ts`, `review-session.ts`), `reviewerRecipe` · `reviewPrompt` · `reviewTitle` · `parseReviewTitle` (`session-controls.ts`), `openFile(path, line)` · `review` · `openReview` on `PaneContext`; `diff-review`, `git-review`, `review-pane[data-state]`, `review-header`, `review-list`, `review-row-<id>`, `review-rerun`, `review-open-transcript`, `review-body`, `review-section-<id>`, `review-link[data-path][data-line]`, `review-raw`, `review-partial`, `review-error`, `review-empty` test ids |
| the review's one-word answer (2026-09-16, task 70) | **verdict** — a chip reading **PASS** (`success`) · **PASS WITH ISSUES** (`warning`) · **FAIL** (`danger`), beside the word Verdict; a reply with none shows as markdown under a bar saying so | verdict | `Verdict`, `parseReview` (`review-parse.ts`); `review-verdict[data-verdict]` test id |
| a session saved to run again (2026-09-16, task 59) | **routine** — **Save as routine…** in Session controls and the ⋯ menu opens the sheet; a run's session carries the **Routine: <title>** chip; the trigger is **Manual · Hourly · Daily · Weekly · Custom cron**, Manual reads "run only when I press Run now" | schedule, recipe | the Schedules route and its `schedule.json` job (Manual = paused), the recipe in the library and `scheduled_recipes/`; `routine-sheet`, `routine-title`, `routine-instructions`, `routine-trigger`, `routine-cron`, `routine-save`, `workspace-save-routine`, `workspace-rail-save-routine`, `workspace-routine` test ids (`src/workspace/routine/`) |
| every unit of work at once (2026-09-16, task 67) | **board** — the `#/board` route, sidebar item **Board**; columns **Running · Needs review · Done**, nothing dragged: a card's column is read from its work | board | `BoardView.tsx`, `board-state.ts` (`BOARD_COLUMNS`); `board[data-state]`, `board-column-<running\|review\|done>`, `board-filters`, `board-filter-<project\|runtime\|status>` test ids (`src/components/board/`) |
| one unit of work on the board (2026-09-16, task 67) | **card** — a session, a **Routine** run, or a **Worker** (a delegated child, nested under its parent's card): title · runtime · `wt/<slug>` (mono) when it has a worktree · Started / Ended · a status dot beside its word (`info` Running, `warning` Needs review, `success` Done, `danger` Failed — Failed sits in Done); click opens the session on `/pair`, a worker its own transcript | card | `BoardCard.tsx`; `board-card[data-kind][data-status][data-session-id]`, `board-card-status`, `board-card-runtime`, `board-card-branch`, `board-card-review-unknown` test ids |
| work waiting on the user (2026-09-16, task 67) | **Needs review** — a run the inbox has not opened since its outcome landed, or a session whose checkout has uncommitted changes; it clears when the run is opened or the changes are committed, never by hand | unread, dirty | `runsInbox.seen` (`runs-state.ts` `isUnread`) and `/git/status` porcelain non-empty for the session's cwd, one call per distinct cwd every 30 s while the board is open (`BoardView.tsx` `reviewCwds`) |

| what a pane hands the chat (2026-09-19, tasks 76, 83, 85, 86, 87, 94) | **Add to chat** — one door, every pane: a quote lands in the chat input at the caret as a fenced block under a header the model can locate — `path:from-to` (Editor's selection, a Changes hunk), `path` (a Markdown selection, a whole file from the Files menu), `terminal` (the Terminal's selected text, its pill reads **Send to chat**), an image tile (Browser ▸ Share ▸ Screenshot); a second quote starts on its own line; nothing is sent — the user reads it first. Disabled with **Select text first** when nothing is selected; the Changes hunk's button reads **Ask about this** | quote, insert | `quoteForChat`, `pathForChat`, `InsertChatInput` (`chat-insert.ts`); `insertIntoChat` on `PaneContext`; `AppEvents.INSERT_INPUT_TEXT` / `INSERT_INPUT_IMAGE` handled in `ChatInput.tsx`; `editor-add-to-chat`, `markdown-add-to-chat`, `terminal-send`, `files-menu-add-to-chat`, `diff-hunk-ask`, `browser-share-screenshot` test ids; walk `add to chat` |
| a turn's file changes, reversible (2026-09-19, task 88) | **Undo this turn** / **Redo this turn** — in the user bubble's action row; Undo puts the files the turn touched back to the tree as it was before the turn and leaves a divider "turn undone" under it; the control then reads Redo. Refused in place, with the reason, when a later turn touched the same files. Paths git ignores are outside it | turn undo | `T0` / `T1` snapshots (`/git/snapshot` on send and on idle), `turn-undo.ts`, `project-storage.ts`; `turn-undo` test id; walk `turn undo` (not yet run: it needs a live seat) |
| the transcript's record of a write (task 93, not landed) | **diff card** — a `write` / `edit` tool row rendering as the change itself, in the Changes pane's renderer; until it lands the tool row shows the call | — | `TurnDiffCard` (planned), `ChangeView` lifted from `DiffPane.tsx` |
| the tree's state, above the composer (2026-09-19, task 84) | **Changes bar** — a row over the chat input while the working tree is dirty and the chat idle: `<n> files · +a −d`, then **Review** (opens Changes), **Accept all** (commits, the message drafted from the turn), **Discard** (restores the tree, with **Undo** in a toast); it hides while the agent writes | changes bar | `ChangesBar.tsx`, `ChangesBarTarget` context (`changes-bar-slot.ts`); `changes-bar-stats`, `changes-bar-review`, `changes-bar-accept`, `changes-bar-discard`, `changes-bar-undo` test ids; walk `changes bar` |
| the Changes tab's count (2026-09-19, task 94) | **badge** — `<n> · +a −d` on the Changes tab off the shared git-status poll; `…` until numstat answers, the file count alone if it fails; a dot on the phone's rail | badge | `PaneChrome.badge`, `useChangesBadge` (`WorkspaceShell.tsx`); `workspace-tab-badge-diff` test id |
| a Changes row's own actions (2026-09-19, task 94) | **Stage file · Discard file** on hover of a file row (Discard with **Undo** in a toast); on a hunk, **Ask about this** (Add to chat) and **Open in Editor** at the hunk's first line; Stage · Reject per chunk stay (task 50) | row actions | `/git/discard {cwd, path}`, `/git/discard-undo`; `diff-row-stage`, `diff-row-discard`, `diff-hunk-ask`, `diff-hunk-open` test ids |
| a long document's map (2026-09-19, task 95) | **Contents** — a collapsible list of the document's headings at the top of the Markdown pane, shown from three headings up; a row scrolls its heading to the top; **Edit** opens the Editor at the heading nearest the current scroll | toc | `headings`, `slug` (`markdown-state.ts`); `markdown-toc`, `markdown-toc-row`, `markdown-edit` test ids; walk `markdown pane` |
| everything the workspace can do, by name (2026-09-19, task 92) | **palette** — ⌘K; one list: the panes, the sessions, the routines (with **Run now**), the lever's stops, the routes, the session menu's actions, ranked by the fuzzy match the provider picker already used; **Type to search…**, "Nothing matches "<query>"" | command palette | `palette-state.ts` (`commands`, `filterCommands`, `PALETTE_STATES`), `utils/fuzzy.ts`; `palette-input`, `palette-list`, `palette-item-<id>`, `palette-group-<name>`, `palette-empty`, `palette-loading`, `palette-tab` test ids; walk `command palette` |
| the seats, checked (2026-09-19, task 91) | **runtimes gate** — **Set up seats**: one row per runtime (Claude · Codex · Cursor · agy) reading **Install** or **Sign in** or Ready, **Recheck**; shown on launch only while no seat is Ready, and reachable later from the lever; a missing binary is Install, a binary that answers signed-out is Sign in — the probe is four fixed argv arrays, never a shell | seat | `POST /runtimes/probe`, `seat-state.ts` (`seatState`, `RUNTIMES_GATE_STATES`); `runtimes-gate-row-<seat>`, `runtimes-gate-install-<seat>`, `runtimes-gate-sign-in-<seat>`, `runtimes-gate-recheck`, `runtimes-gate-done` test ids; walk `runtimes gate` |
| Orchestrate's pause before code (2026-09-19, tasks 79, 90) | **plan gate** — on by default in Orchestrate: the orchestrator ends its turn once the Planner returns, the RPI strip's Plan chip reads **Plan ready — waiting for you** with **Accept · Revise…**; Implement lighting without Accept raises a `warning` bar "Implement started without your Accept" with **Stop**; the setting is per project; a routine saved from the session does not carry the gate | plan gate | `PLAN_GATE_LINE`, `orchestratorRecipe(role, planGate)` (`session-controls.ts`); `workspace.planGate` setting; `.agents/agents/orchestrator.md` §Task sizing; `rpi-gate`, `rpi-accept`, `rpi-revise`, `rpi-overrun`, `rpi-stop` test ids; walk `plan gate` (task 96, not yet written) |
| which model answered, what it cost, whether the roles earn their keep (2026-09-20, tasks 125–133; user: "which models are being called with what settings" · "dashboards! graphs! time over days, months, quarters" · "are the roles continuing to be effective?" · "no prose, just numbers and charts … hover … why it is important" · "at the top, 3 trends in prose bulleted form") | **Telemetry** — one tab; three scopes **Now · Over time · Roles** and a grain **Days · Weeks · Months · Quarters** (the chip `14 d · 13 w · 12 mo · 5 q`); the **Trends** card's three sentences are the only prose on the pane — every other number carries a hover: *why it matters*, then *where it reads from*. **The Diagnostics-tier exception:** this is the one pane where a provider id and Goose's permission gate show on a default surface, in mono under the value (Now's Session card) — the Named Runtime Rule holds everywhere else | telemetry | `TelemetryPane.tsx`, `TELEMETRY_STATES` (`telemetry-state.ts`), `Why.tsx` (the one tooltip); `telemetry-pane[data-scope][data-grain]`, `telemetry-scope-<now|time|roles>`, `telemetry-range-<days|weeks|months|quarters>`, `telemetry-range-label`, `telemetry-why`, `telemetry-trends`, `telemetry-trend`, `telemetry-now-*`, `telemetry-time-*`, `telemetry-stack`, `telemetry-roles-*`, `telemetry-routing`, `telemetry-role-trend` test ids; walk `telemetry pane` |
| one reply from a model (2026-09-20, task 129) | **turn** — the unit Telemetry counts: the assistant message carrying usage, one per reply; on Claude · Codex · Cursor · agy a turn is one prompt to that CLI, whose own calls never show. Never "call" or "request" on the surface | turn | `turnEvents`, `turnsOf` (`ledger-events.ts`, `telemetry-now.ts`) |
| the app's record of what the agents did (2026-09-20, task 126) | **ledger** — one line per moment the app already knows: a turn's usage landed, a worker returned, the session edited a worker's file, a verdict parsed, a turn was undone; every Telemetry chart is a fold over it | ledger | `<state>/goose/ledger/<repo>-<hash>.jsonl`; `POST /ledger/append` · `/ledger/read` (`ui/sidecar/src/ledger.ts`); `native/ledger.ts`; `ledger-events.ts`, `ledger-writer.ts` |
| a worker's work the session redid (2026-09-20, task 131) | **corrected** — the session wrote to a file the worker returned under Files Changed, after Done; **blocked** — the worker answered BLOCKED (a plan failure, not a seat failure); **clean-done** — done, not blocked, not corrected, over runs. Done alone lies (a BLOCKED return and a rewritten diff both read Done on the wire); Corrected decides | correction | `correctionEvents`, `isBlockedReturn` (`ledger-events.ts`); `roleRows`, `verdictOf` (`telemetry-roles.ts`) |
| the board's one-word answer per role (2026-09-20, task 131) | **Effective** (`success`) — clean-done ≥ 80% and PASS ≥ 80% when attributed · **Watch** (`warning`) — below either · **Failing** (`danger`) — corrected ≥ half the runs, or clean-done under 50% | verdict | `VERDICTS`, `verdictOf` (`telemetry-roles.ts`); `telemetry-roles-row[data-verdict]` |
| a number the wire cannot see (2026-09-20, task 131) | **hand-counted** — a dashed pill on a row whose runs never passed through `delegate` (the session's own subagents) and whose count comes from `tasks.md`; nothing fills it yet (§Waiting) | hand | `RoleRow.hand` (`telemetry-roles.ts`) |
- Labels are imperative verbs in sentence case: Install, Sign in, Locate…, Restart, Commit, Tear off (PRD §States).
- Errors: the cause in plain words, then the action that fixes it — "[exited <code>] — Restart", "No changes vs <base>", "Not a git repository" (PRD `:157-163`); never "something went wrong".
- Runtimes are named as the user says them (Claude, Codex, Cursor, agy); a provider id never rises to the default surface.
- "Mode" in the workspace means Direct · Orchestrate only; the permission gate keeps upstream's words and stays in Settings.
- Pane names are nouns; a tab reads Files, never "File browser".
- Retired: **Diff** → **Changes** (user, 2026-09-15; task 40) — the pane shows what changed, "diff" names the artifact; the word survives only as the id `diff` (`pane-store.ts:4`) and the `diffPane.*` message ids, never on a surface. A retired word goes to `docs/decisions/`, dated; that directory does not exist yet, so this line is the record until it does.
- Retired: **header**, **top bar** (task 60; user: "please remove the top navbar") — the frame has no bar over the window; Runtime and Mode are chips. 2026-09-16 (task 71): the Work column's **tab bar** is a bar on the column, not over the window — "navbar" in the user's words, "tab bar" here.
- Retired: **side panel**, **beside the chat**, **Open as pane** (task 42) — the dock replaced the side panel and the centre pane; "side" survives only in the `workspace-side-panel` / `workspace-side-tab-*` test ids, never on a surface.
- Retired: **rail** (2026-09-16, task 71; user: "have a permanent navbar at the top that creates the tabs") — the floating launchers became the tab bar; the word survives only in `RailMenu.tsx`'s file name, the `rail.menu` message id and the `workspace-rail-save-routine` test id, and for the phone's **tab rail**, never on a desktop surface. **panel** as a stack, **Tear off**, **Move up**, **Move down** (task 71) — the N-panel dock became the three dock positions; "dock" survives only in **dock position**, the `dock` walk and the `goose.workspace.dock` storage key.
- Retired: **Duplicate** → **Fork** (2026-09-16, task 69; Claude Code desktop's word) — the session menu's row and its toasts say Fork; the word survives only in upstream's header menu (`sessionActionsHeader.duplicateSession`, hidden on the workspace routes) and the Session History's own row, never on a fork-side surface. **More panes** → **Session menu** (task 69) — the ⋯ is the session's menu, not a second pane list.
- check: open — see §Open decisions (no workspace strings exist to check yet).

## Shared component states [contract]

The PRD's per-surface lines (`:137-170`) are deviations from these rows; a state the PRD names is one of these.

| State | Renders | Actions available | Context retained | Focus lands | Announces |
|---|---|---|---|---|---|
| Empty | the surface's purpose, the path or base it looked at, one starting action ("Nothing here" + path; "No changes vs <base>"; "No delegated work yet") | the starting action | — | the action | the empty line |
| Loading | layout preserved; the unresolved part is a skeleton or a row spinner, never a blank | navigation, tab switch, Stop | all | unchanged | "loading" once, then silence |
| Partial | the resolved part is live; the unresolved part carries one icon or one bar naming what is missing (lock, reload bar, "binary", "Sign in") | everything on the resolved part; the bar's action | all | unchanged | the bar's text |
| Running | the row keeps its spinner while the call runs, even after the reply ends (PRD `:148-149`); Commit is disabled and says why (PRD `:79`) | Stop; navigation | all | unchanged | the row's title |
| Error | plain cause (git's stderr, the OS error, the provider's error) then the recovery it names; the row stays (PRD `:167`, `:170`) | the recovery | input and selection kept; chat input enabled | the recovery | cause, then recovery |
| Cancelled | the reply truncated where it stopped; input enabled (PRD `:54`) | send again | the draft | the input | "stopped" |
| Unavailable | the row stays and reads Install, Sign in, or the probe's one line; the session does not start (PRD `:40-47`, `:142-146`) | Install · Sign in | selectors unchanged | the row | the row's text |

- A closed pane shrinks into its tab, which stays on the bar as a launcher, and focus lands on that launcher (`WorkColumn.tsx` focusAfterClose; no tab's tooltip opens on focus); the other half, if any, takes the column (`pane-store.ts` closePane, `pane-store.test.ts`). Until task 71: into its strip, the emptied panel into the panel above, focus on the rail's ⋯.
- Amended 2026-09-16 (tasks 49, 50, 53): the Changes pane's Reject · Stage · Undo, its Merge · Remove worktree, and the Runs inbox's Accept · Dismiss are Running-row controls — disabled in place with the reason while a tool call or the run is in progress, never hidden and never behind a confirm (§Accessibility "Destructive"); a refused `/git/apply` or a merge conflict is the Error row: git's stderr or the conflict list, the chunk or the row kept, and the recovery named in place — Undo until the next apply, "Resolve these in the Editor or the Terminal, then merge again".
- Amended 2026-09-16 (task 70): the Review pane declares `REVIEW_PANE_STATES` (`review-state.ts`, the rows above plus `ready`) — empty → "No review yet — Review branch… in Changes"; loading → "Reviewing…" over what has streamed so far (the export's "Loading…" for a session the app has restarted since); partial → a reply with no verdict shows as markdown under a one-line bar saying so, a finished session with no reply says that; error → the session's error with Re-review (Retry for an export that failed); Re-review is disabled while the reviewer writes, never hidden.
- Amended 2026-09-16 (task 67): the Board declares `BOARD_STATES` (`board-state.ts`, the rows above plus `ready`) — empty → "Nothing running — start a chat" and the line that a column is read from the work, never dragged; loading → three skeleton columns; partial → a session whose cwd the sidecar refuses (outside its repository, or no sidecar) keeps its card with a one-line note in place of the review check; error → the list call's cause with Retry.
- Amended 2026-09-18 (task 90): the RPI strip declares `RPI_STRIP_STATES` with a new **awaiting** state (`rpi-strip-state.ts`, plan gate) — awaiting → Plan phase done and lit, Implement phase dim, chat idle, gate on: the Plan chip carries a bar "Plan ready — waiting for you" with Accept · Revise… actions; the Artifact pane's header row mirrors both; focus and context unchanged; announces the bar's text. If Implement lights while awaiting and gate is armed, a `warning` bar appears instead: "Implement started without your Accept" with Stop. An overrun bar is not a state: it rides atop the awaiting UI and disappears when Implement dims again.
- Amended 2026-09-20 (tasks 128–133): the Telemetry pane declares `TELEMETRY_STATES` (`telemetry-state.ts`, the rows above plus `ready`) — empty → the scope's line ("No turns yet — send a message and the model that answers shows here" · "No turns in this range — widen it" · "No delegated work in this range"; Trends: "No movement yet — two ranges of turns are needed"); loading → the cards keep their frame, Trends three skeleton lines, "Reading the ledger…"; partial → a Cost that reads `est.` or `—`, the unpriced count in the headline's line, a PASS rate the hover names as branch-level; error → the read's cause with **Retry**, the last good picture kept.
- Amended 2026-09-19 (tasks 89, 91): **waiting on the user** is Partial, task 90's shape — the resolved part live, one bar naming who is waited on: the tool confirmation card's "waiting for <seat>" bar over Allow once · Always allow · Deny (`TOOL_CONFIRMATION_STATES`, `tool-confirmation-state.ts`: the rows above plus `ready` for a decision that landed; the card shows the adapter's own tool title and the diff it forwarded), the RPI strip's `awaiting` (above), the runtimes gate's rows (`RUNTIMES_GATE_STATES`, `seat-state.ts`: Install and Sign in are Unavailable rows that stay; `partial` while some seats answered). Session controls' Mode radios carry a line naming what the current seat does with Approve and, under it, the seat's own refusal when a mode change fails (`workspace-config-mode-note`, `workspace-config-mode-error`).
- check: open — see §Open decisions (no pane declares a state type yet).

## Tokens & theme [contract]

Upstream's, unchanged. Source: `ui/desktop/src/theme/theme-tokens.ts` (named the single source of values at `ui/desktop/src/styles/main.css:62`) — roles `background`, `text`, `border`, `ring` × `primary`, `secondary`, `tertiary`, `inverse`, `ghost`, `info`, `danger`, `success`, `warning`, `disabled` (`main.css:68-110`); shadows `--shadow-hairline`, `--shadow-sm`, `--shadow-md`, `--shadow-lg` (`theme-tokens.ts:141-144`, dark `:197-200`) and `--shadow-default` (`main.css:203`, `:230`); themes light and dark (`main.css:181`, `:208`). Delta:

- Colour: `info` marks what the agent touched (the Files dot, PRD `:58`); `success` a done worker or phase; `warning` a waiting one; `danger` a failed one that stays (PRD `:104`, `:170`). Semantic roles never retint with theme; runtime identity has no colour role (open).
- Elevation is the Floating Button Rule: a workspace control at rest carries `--shadow-sm`; lifted (hover, drag, in flight during an Into motion) carries `--shadow-md`; `--shadow-lg` and `--shadow-default` stay upstream's for overlays. Upstream's own buttons are flat — `button.tsx:11` asks for `shadow-xs`, which the reset at `main.css:25` removes and `theme-tokens.ts` never defines — so the rule is a real delta; whether it lands as a `button.tsx` variant or a workspace wrapper is the plan's.
- Space and radius: upstream's scale for space. Radius — amended 2026-09-16 (user: "large radiuses"): the fork adds one step, the same shape in every theme — `--radius-panel` 16px (cards, menus, message bubbles), `--radius-control` 12px (buttons, the `button.tsx` pill shape), `--radius-chip` 999px (`main.css` `:root`/`.dark`, utilities `rounded-panel` / `rounded-control` / `rounded-chip`).
- 2026-09-16 delta (task 57): **Charcoal Monokai** — the dark theme itself, not a fourth theme (user: "switch the dark theme over to dark charcoal monokai"); light and aura stay byte-for-byte (`theme-tokens.test.ts` snapshots). Ground `#1c1c1c`, secondary `#242424`, raised `#2c2c2c`, each at .8 alpha so the charcoal reads as charcoal over the main window's macOS vibrancy (`main.ts` `vibrancy: 'window'`); off macOS the canvas under `body` (`main.css` `body` is the only painted root) is the window's own, so a dark canvas there is open (§Open decisions); text `#f8f8f2` / `#c8c8c2` / `#7a7a72` — tertiary for hints and disabled only, never body copy. The Monokai accents on the semantic roles: `info` `#66d9ef`, `success` `#a6e22e`, `warning` `#e6db74`, `danger` `#f92672`, accent (`background-inverse`, `ring-primary`) `#ae81ff`; orange `#fd971f` has no role yet, so the Files dot keeps `info`. `border-*` tokens are `rgba(248,248,242,.06)` hairlines and the shadows deep (`--shadow-sm` `.5`, `--shadow-md` `.65`): surfaces float (§Principles). Contrast is measured over `#1c1c1c` (`theme-tokens.test.ts`): primary and secondary text meet AA on every surface, hint text and the status colours 3:1. Glass (§Principles Glass Rule) is `backdrop-filter: blur(18px) saturate(1.3)` in `main.css` "Charcoal Monokai" on `.workspace-column` (task 60; `.app-sidebar`, `.workspace-header`, `.workspace-dock-panel` until then; `.workspace-rail-button` until task 71), `.chat-input-card` and `[data-slot='dropdown-menu-content']`; the block is scoped `[data-theme='dark']` so aura's own bubble override keeps its precedence. Syntax and terminal palettes follow the variant: `theme/monokai-highlight.ts` for the Editor and Changes panes (inert in Changes until that pane loads a language, `DiffPane.tsx` `ChangeView`), the canonical ANSI eight (`#272822 #f92672 #a6e22e #f4bf75 #66d9ef #ae81ff #a1efe4 #f8f8f2`) on the charcoal ground for the Terminal (`terminal-session.ts`) — aura, also a dark variant, inherits both.
- 2026-09-18 delta (tranche 8, tasks 98–101; user: "update the UI to look and behave like the compo reimagined in ux_tests"): **Light is the Studio board** — ux_tests's B · Studio — teal & amber (`ux_tests/src/index.css` `.studio`, `docs/2026-09-18-studio-board-plan-v1.md`), not a fourth theme. `lightColorTokens` (`theme-tokens.ts`): page `#ffffff`, inspector ground `#f8f7f3`, sidebar `#f1efe9`, ink `#1e1d1a` / `#5a5851` / `#6f6c64`, the one hairline `#e3dfd6`; roles: `info` blue `#2277cc` (where you are, focus), `background-inverse` teal `#0b7a72` with white text (the board's `#44c1b8` reads 2:1 under text, so filled controls take the darker cut; the Send disc keeps `#44c1b8` under an icon), `success` `#2f8f5b`, `danger` `#c23b3b`, `warning` amber `#fbbf24` (a mark; `#d9a012` as text); shadows are the board's three (`--shadow-sm` buttons and rows, `--shadow-md` the composer and cards, `--shadow-lg` menus). `theme-tokens.test.ts` holds Light's text at AA on the accent and on every light ground. Treatments live in `main.css` under `[data-theme='light']`, the Monokai block's sibling: the Sessions column frosted ivory at 78 % over a 48 px blur (the Glass Rule), the Work column on the inspector ground, the composer floating without an outline, your message and the tool rows on the tint `#f3f8fd` (no role — a treatment), the agent's reply flat, an ask the one white surface on a hairline, the active tab white with a blue icon, the current session row white at 600, the running spinner a blue dot. Hooks the fork added for it: `.work-tab[data-active]`, `.send-disc[data-armed]`, `.tool-row`, `.ask-card`, `.session-row[data-active]`, `.running-dot`, `.workspace-column[data-column]`. Not built: idle tabs shrinking to icon + kind (a tab-bar behaviour), the file line's amber bar (no hook in the diff renderer). Monokai's and Aura's colours are unchanged (their colour snapshots).
- 2026-09-19 delta (tasks 85, 94): **git tints** — a path's git status colours its text in the Files tree and the Changes list, and the word rides beside it for the screen reader: `M` `warning` (Modified), `A` and `?` `success` (Added, Untracked), `D` `danger` (Deleted); a clean path takes no tint (`FilesPane.tsx` `gitTint`, `gitColorClass`). The Changes badge and bar count in ink, not tint: the numbers are a measure, not a state.
- check: every role named here resolves — `for r in shadow-sm shadow-md shadow-default color-text-info color-text-danger color-text-success color-text-warning; do grep -q -- "--$r" ui/desktop/src/theme/theme-tokens.ts ui/desktop/src/styles/main.css || echo "missing $r"; done` prints nothing.

## Typography

The Studio board's (2026-09-18, task 98): Schibsted Grotesk Variable and JetBrains Mono Variable, self-hosted (`@fontsource-variable/*`, imported at the top of `main.css`; Cash Sans's CDN fetch is gone), on the five-size scale in every theme (Aura keeps its monospace face — `auraFontTokens` — and takes the scale) — `text-xs` 11/16 · `text-sm` 12/16 · `text-md` 13/20 (body, buttons, list items) · `text-lg` 15/22 (titles) · `heading-xs` 20/28 (one per screen); weights 500 regular, 600 emphasis (`theme-tokens.ts` base tokens; `main.css` resets Tailwind's font namespaces and an `@theme inline` block maps `text-xs … text-xl` onto the size tokens — before 2026-09-19 nothing read them). Delta: mono is for pane contents that are text from the machine — terminal, diff, editor, code, git output — and for nothing else; a tab, a header control, or a worker row in mono is a bug. Amended 2026-09-16 (task 28): a worker row's runtime · model is the child record's provider id and model id — machine text, mono, as the Worktree chip's `wt/<slug>` and the PR `#<number>`; the row's title, role and status word stay in the body face.

## Iconography

Upstream's: `lucide-react` (`ui/desktop/package.json`), used by `components/ui/*`. The fork's tabs, pane toolbars and the tab bar draw from the same set at upstream's control size. Amended 2026-09-20 (tasks 122–124, user: "iconography … consistent with the ux_tests"): the **composer row** draws from **Heroicons 16 solid** (`@heroicons/react/16/solid` — ux_tests' one set, `DESIGN_LANGUAGE.md` §3.1): cube for the model, folder, paper clip, arrow up, 14 px in chips and 16 px on buttons, inheriting the text colour beside them. Two sets is a known state, not a rule: the row is the first surface moved; the rest of the app moves when a later plan names it, and no new surface adds a third. One icon per pane wherever it is shown (`WorkspaceShell.tsx` `PANE_ICONS`): Files `FolderTree` · Editor `FileCode` · Changes `GitCompare` · Terminal `Terminal` · Git `GitBranch` · Browser `Globe` · Markdown `BookOpen` · Agents `Users` (task 28); the More button is `Ellipsis`. The chips (task 60): Runtime `Cpu` · Mode `Workflow`, beside upstream's model `Bot` and directory `FolderDot`; a narrow chat input shows the icon alone. Task 58: the lever's knob is a `Gauge`, the Session controls chip a `SlidersHorizontal`, the ⋯ menu's checked "Advanced controls" a `Check`. Task 49: the Worktree chip is a `GitBranch`, in Easy and Advanced alike. Task 59: "Save as routine…" in the ⋯ menu and the Routine chip are a `Repeat`. Task 62: the ⋯ menu's "Open on phone…" is a `Smartphone`; the Phone card's Copy is a `Copy` (a `Check` once copied) and its code carries a `QrCode` mark — the code itself is black on white whatever the theme, since a scanner wants dark on light. The Browser's toolbar (task 56): Back `ArrowLeft` · Forward `ArrowRight` · Refresh `RotateCw` (Stop `Square` while loading) · Share `Share`. Task 66: a PR check is a `CircleCheck` (`success`) when it passed, a `CircleX` (`danger`) when it failed, a `CircleDashed` while pending (`warning`) or when skipped (tertiary text) — each beside the check's name and word, never alone (§Accessibility). Task 69: the session menu's rows — Background tasks `Inbox` (a `Kanban` once the Board lands, task 67) · Open in ▸ `FolderOpen` · Rename `Edit2` (upstream's) · Fork `GitFork` · Transcript view ▸ `AlignJustify` (View session JSON `FileJson`, View recent model interactions `Activity`, upstream's) · Output style ▸ `Palette` · Keep computer awake `MoonStar` (a `Check` while on) · Archive `Archive` · Delete `Trash2` in `danger`; the tab's status dot is a dot, not an icon, paired with its tooltip's words. Task 67 (reconstructed 2026-09-22 from a garbled merge of the two sentences, `9051fb687`): the sidebar's Board item and the board's empty state are a `Kanban`; a card's runtime is a `Cpu`. Task 71: the Filthrese dock positions — Full `PanelRight` · Top half `PanelTop` · Bottom half `PanelBottom` — on the pane header's control and the tab's right-click menu, the pressed one where the pane shows; the bar's overflow is a `ChevronDown`. Task 70: the Review pane is a `ClipboardCheck`; the verdict is a text chip in its colour role, no icon. The Files dot is a dot, not an icon, and is always paired with text (§Accessibility). Task 28: a worker row's status is the same dot — `info` running, `success` done, `danger` failed (stays), `warning` waiting — beside its word, never alone; a row with no status on the wire (seeded from the session record on reload) carries a `CircleDashed` in tertiary text with "No status recorded"; the transcript's Back is an `ArrowLeft`. Runtime identity has no colour role (§Open decisions). Amended 2026-09-21 (task 152, user: "we still don't have a melody emblem" → seven sheets → "i like h5" → "is there an emblem too similar" (Ericsson's three stacked slanted capsules is the cousin) → "show me h3 but on the same horizon line" → "let's do i4"): **the mark is three bars on one horizon** (canonical: `ux_tests/docs/melody-style-guide.html`, the shared copy `ux_tests/public/icons.svg#melody-emblem`; this line is the pointer, the guide owns the geometry and the don'ts) — three equal bars of 10 units at 45°, 3.2 wide with round ends, centred at 6 · 12 · 18 on one line, on the 24-unit grid; one shape, `currentColor` fill, no stroke, no gradient, no illustration (ux_tests `DESIGN_LANGUAGE.md` §3.1: "an icon is a mark beside a word, never an illustration"). Three sources carry it and nothing else draws it: `components/icons/Goose.tsx` (the in-app mark — the loader, `LoadingGoose` on Idle, a recipe's activities; the export keeps upstream's name so its import sites stay untouched), `images/glyph.svg` and `glyph-update.svg` (the 22/44 px menu-bar template, the update variant's red dot at 21,3 clear of the top bar), `images/icon.svg` (the mark at ~69 % inside upstream's white rounded square — the dock, the installer, the `.ico`); every raster is `images/prepare.sh`'s output, which renders through `qlmanage` and `iconutil` and packs the `.ico` itself — macOS only. At 22 px the gaps between bars are ~1.9 px. Neighbours checked 2026-09-21: Ericsson (stacked, ~18°), what3words (`///`, steeper and boxed), adidas (rising heights), Windsurf (curves) — same family, none the same mark. The product name, `Goose.app` and upstream's goose animations (`AnimatedIcons`, `FlyingBird`, `Rain`) are not the mark's and were left as they are.

## Motion

Upstream's easing role `--ease-g2` (`main.css:65`) and, since 2026-09-18 (task 101), four duration roles in `main.css :root`: `--motion-fast` 120 ms (lift, hover), `--motion-press` 80 ms (press), `--motion-base` 180 ms (tab colour, menus, rows landing), `--halo-period` 1600 ms (the running dot). The Studio board's beats, in every theme: filled and outline buttons lift 1 px and widen their shadow on hover, drop on press (`button.tsx` `lift`; ghost and link stay flat); the running dot breathes a halo to 8 px; the Send disc steps up 3 px once the composer has text (`data-armed`); tool rows land with a 4 px rise; menus pop in over `--motion-base` − 20 ms and leave in 100 ms. `prefers-reduced-motion: reduce` stills all of it. Every animation names the continuity it explains; here each is the Into Rule applied to the pane store:

- Open and dock (`openPane`, `dock`, `pane-store.ts`): the pane grows out of its tab — `animate-in fade-in-0 zoom-in-95 origin-top` over `--ease-g2`, 150ms (`WorkColumn.tsx` body classes; `Dock.tsx` until task 71); a parked tab's pane replays it when shown.
- Drag (`WorkColumn.tsx` ghost; `Dock.tsx` until task 71): the tab in flight is a lifted copy under the pointer (`--shadow-md`, the Floating Button Rule), the source fades to half, the drop zone — the column's top half or bottom half — lights under the pointer; on release the layout changes in place.
- Close (`closePane`, `pane-store.ts`): the pane shrinks toward its tab (`animate-out zoom-out-95`, `WorkColumn.tsx` CLOSE_MOTION_MS) and only then leaves the store; focus lands as §States says.
- Columns: the Sessions column eases shut into the titlebar toggle (`transition-[width]`), a seam drag moves with the pointer. Until task 71 the rail's launchers shared a `layoutId` in both homes and slid between the window's edge and the top panel's strip; the tab bar has one home (task 71).
- Phone (`show`, `:171-175`): the leaving pane returns into its rail tab; the arriving one grows out of its rail tab; chat is a rail tab like the others.
- Lever (task 58, `Lever.tsx`): the knob and its label slide to the picked stop (`transition-[left]`, 150ms, `--ease-g2`) — the Into Rule on a track.
- The "→ <Runtime> from here" divider appears in place; nothing animates in the transcript, the tool rows, or the terminal — text streams and rows appear, that is all.
- Reduced motion: upstream's global block zeroes every transition (`main.css:380-392`), so an Into motion degrades to a cut; the focus rules in §States still hold, so only the picture is lost.

- Amended 2026-09-20 (user: "when the mouse hovers over the chat panel, raise the prompt"): the composer card rises 3 px to `--shadow-lg` while the pointer is anywhere over the Chat column and settles when it leaves (`--motion-base`); the send disc no longer nudges when text arrives (the `send-nudge` keyframes are gone) and keeps only its own 1 px hover lift; typing moves nothing.

## Accessibility & input

Upstream's, unchanged. Delta for the workspace:

- Promote, close, and every tab switch are reachable from the keyboard; after a close, focus is on the tab the pane returned into (§States).
- Keys the PRD fixes: ⌘Enter sends (`:50`), ⌘S saves the editor (`:61`), Esc cancels the turn (`:54`) — Esc is therefore never a pane-close key (open). Task 60: ⌘1 · ⌘2 · ⌘3 focus Sessions · Chat · Work; a seam is a `separator` that arrows resize.
- Colour never alone: the Files dot pairs with a tooltip or the row's text; a worker's status is a word beside its colour; a runtime is its written name.
- Disabled controls say why in place — Commit while an agent turn writes files (PRD `:79`), Git in a non-repo (PRD `:162`).
- Destructive: none in V0 (view-only diff, PRD `:202`); Stop is not destructive and needs no confirmation.
  - Amended 2026-09-16 (task 50): the Changes pane's Reject and Stage act on the working tree and the index per chunk, with no confirm — the pane keeps the last patch git applied and offers **Undo** in place (the same patch, reversed) until the next apply; a patch that no longer matches is refused whole by git and its stderr is the Error row (`panes/diff/DiffPane.tsx`, `diff-store.ts` `lastApply`).
  - Amended 2026-09-19 (tasks 84, 88, 94): **Undo this turn**, the Changes bar's **Discard**, and a row's **Discard file** act on the working tree with no confirm; each leaves its reverse in place — Redo on the bubble, Undo in a toast — until the next change to the same files; **Accept all** commits and is not destructive. Delete in the session menu keeps its confirm (task 69).
- Conventions: macOS first (⌘); the phone has no modifier keys, so the terminal shows a key bar — Esc · Tab · Ctrl · arrows · paste (PRD `:129-130`; PRODUCT.md §11).

## Ownership

- **This file owns:** frame, vocabulary and copy rules, shared component states, token roles and theme rules, type, icon and motion direction, accessibility and input rules — for `src/workspace`. `src/components` is upstream's and is not restyled (`ARCHITECTURE.md:86`); where a workspace control is an upstream component, this file's rules govern how the workspace places and wraps it.
- **`PRODUCT.md` owns:** promise, users, principles, modes, runtimes and roles, workspace vision, scope tiers (PRODUCT.md §14).
- **`ARCHITECTURE.md` owns:** modules, dependency direction, invariants, data flow (`ARCHITECTURE.md:110`).
- **Feature PRDs own:** one journey's observable behavior; they cite this file's names and states and add only deviations.
- **`theme-tokens.ts`, `components/ui/`, `docs/mockups/` own:** values, implementations, pictures — subordinate to this file's roles and rules; a mockup that contradicts this file is a bug in the mockup.
- **`tasks.md` owns:** the open work; this file never lists tasks.
- **Precedence on conflict:** this file's rules > token values > implemented components > mockups > PRD prose.
- **Change trigger:** an edit to a `[contract]` section reopens every PRD that cites the changed row and reruns the section's `check:`.

## Open decisions

- Vocabulary check: upstream's strings are react-intl `defaultMessage`s (`ModeSelectionItem.tsx:10`); the natural check greps `src/workspace/**` messages for a word outside the table, but no workspace `.tsx` exists yet, so it would pass vacuously — resolved when task 11's header lands and the grep has strings to fail on (the header is chips and a rail since task 60, the rail a tab bar since task 71; the grep stands).
- States check: no pane declares a state type; `pane-store.ts:4-23` types layout only — resolved when the first pane (Files) declares one and a test diffs it against the table.
- Floating Button reach — resolved 2026-09-18 (task 101, approved): the rule reaches upstream's filled and outline buttons through `button.tsx`; ghost and link controls stay flat.
- Motion scale — resolved 2026-09-18 (task 101): four duration roles in `main.css :root` (§Motion).
- Runtime identity colour: whether a runtime gets a colour role beyond its written name — resolved by a user test once the Agents tree shows several runtimes at once.
- Pane-close key: Esc is cancel-turn (PRD `:54`) — resolved by trying candidates in the landed shell.
- Phone breakpoint: `PHONE_MAX_WIDTH_PX` (`pane-store.ts:10`) and upstream's `--breakpoint-md` (`main.css:15`) differ — resolved by measurement on the phone (PRD step 13).
- Dark canvas off macOS (2026-09-16, task 57): the dark surfaces are .8 alpha over the window's canvas; on Windows/Linux Electron's default canvas is white and the phone web build's is the browser's, so the charcoal would read as grey there — resolved by the orchestrator's pick between an opaque `html` background in the Charcoal Monokai section (forfeits the vibrancy bleed) and a platform-keyed `backgroundColor` in `main.ts` plus a root background for the web build. Whether the macOS vibrancy shows through at all is unconfirmed: native captures over a bright desktop showed flat charcoal (task 57 report).
